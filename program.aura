JMP start

val_a:
	DW 300				;Declare word (1, 44)
val_b:
	DW 1000 			;Declare word (11, 232)
val_result:
	DW 0

handle_carry:
	INC A 				;Increment A by one
	RET

start:
	READ val_a + 1, A
	READ val_b + 1, B

	ADD A, B			;Carry flag will become high
	WRITE D, val_result + 1
	
	READ val_a, A
	READ val_b, B

	MOV 1, C
	AND CR, C, D		;Check if Control Register's carry bit is 1
	TEST D = 1			
	JF no_carry
	CALL handle_carry
	no_carry:

	ADD A, B
	WRITE D, val_result
	HALT